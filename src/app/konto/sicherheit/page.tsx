import SicherheitClient from "./SicherheitClient";

export const metadata = { title: "Sicherheit | Mein Konto" };
export const dynamic = "force-dynamic";

export default function SicherheitPage() {
  return <SicherheitClient />;
}
